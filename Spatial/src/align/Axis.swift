import Foundation
/**
 * Axis alignment
 * - Note: Use .rawValue if you want to get the string "topLeft"
 * - TODO: ⚠️️ Consider renaming these to .hor and .ver
 */
public enum Axis:String {
   case horizontal
   case vertical
}
